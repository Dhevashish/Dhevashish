const reduced=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const reveal=document.querySelectorAll('.chapter,.learning-path,.path-step,.build-item,.mind-heading,.constellation-board,.edu-result,.result-pill,.school-note,.football-copy,.football-art,.now-card,.closing,.manifesto');reveal.forEach((el,i)=>{el.setAttribute('data-inview','');el.style.transitionDelay=`${Math.min(i%5*70,280)}ms`});if(reduced)reveal.forEach(e=>e.classList.add('seen'));else{const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('seen');io.unobserve(e.target)}}),{threshold:.1});reveal.forEach(e=>io.observe(e))}const progress=document.querySelector('.progress');const update=()=>{const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=`${max>0?scrollY/max*100:0}%`};addEventListener('scroll',update,{passive:true});update();const glow=document.querySelector('.cursor-glow'),heroArt=document.querySelector('.hero-art');if(!reduced&&matchMedia('(pointer:fine)').matches)addEventListener('pointermove',e=>{if(glow){glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'}if(heroArt){const x=(e.clientX/innerWidth-.5)*7,y=(e.clientY/innerHeight-.5)*7;heroArt.style.transform=`translate3d(${x}px,${y}px,0)`}});else if(glow)glow.remove();const links=[...document.querySelectorAll('.nav nav a')],targets=links.map(a=>document.querySelector(a.getAttribute('href'))).filter(Boolean);new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)links.forEach(l=>l.classList.toggle('active',l.getAttribute('href')==='#'+e.target.id))}),{rootMargin:'-35% 0px -55% 0px'}).observe(targets[0]);targets.slice(1).forEach(t=>document.querySelector('.nav nav a[href="#'+t.id+'"]')&&new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)links.forEach(l=>l.classList.toggle('active',l.getAttribute('href')==='#'+e.target.id))}),{rootMargin:'-35% 0px -55% 0px'}).observe(t));if(!reduced&&matchMedia('(pointer:fine)').matches)document.querySelectorAll('.path-step,.now-card').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=((e.clientX-r.left)/r.width-.5)*2.5,y=((e.clientY-r.top)/r.height-.5)*-2.5;card.style.transform=`perspective(900px) rotateX(${y}deg) rotateY(${x}deg) translateY(-6px)`});card.addEventListener('pointerleave',()=>card.style.transform='')});const secret=document.querySelector('.secret-trigger');if(secret)secret.addEventListener('click',()=>{document.body.classList.toggle('secret-mode');secret.textContent=document.body.classList.contains('secret-mode')?'okay, you found it.':'made while figuring things out.'});